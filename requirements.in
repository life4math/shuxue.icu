# 直接依赖清单。生产 Python 3.11 锁文件更新命令：
# uv pip compile requirements.in --python-version 3.11 \
#   --python-platform x86_64-manylinux2014 --generate-hashes \
#   --output-file requirements-py311.lock
#
# 迁移期保留的 Python 3.8 回滚锁文件：
# uv pip compile requirements.in --python-version 3.8 \
#   --python-platform x86_64-manylinux2014 --generate-hashes \
#   --output-file requirements-py38.lock
Flask==3.0.3
gunicorn==23.0.0
pdfplumber==0.11.5
mammoth==1.12.0
openai==2.2.0
SQLAlchemy==2.0.51
psycopg2-binary==2.9.10
alembic==1.13.3
pytest==8.3.5
